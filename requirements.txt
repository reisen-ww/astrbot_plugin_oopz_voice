# NOTE: `oopz-sdk` is NOT listed here due to a cryptography version conflict.
# `oopz-sdk 0.13.x` pins `cryptography<48`, but AstrBot core requires
# `cryptography>=48`. We install it automatically via `pip install --no-deps`.
# To install manually: `pip install oopz-sdk --no-deps`
#
# NOTE: `webrtcvad` is also NOT listed (no pre-built wheels for Python 3.12).
# We use `pydub` for silence detection instead.
aiohttp>=3.9.0
pydub>=0.25.1
numpy>=1.24
faster-whisper>=1.0.0
